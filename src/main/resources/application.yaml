spring:
  application:
    name: backend
  data:
    mongodb:
      uri: mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@cluster0.ujgb2fn.mongodb.net/mydb?retryWrites=true&w=majority

  jwt:
    secret: ${JWT_SECRET}
    access-expiration: ${JWT_ACCESS_EXPIRATION}
    refresh-expiration: ${JWT_REFRESH_EXPIRATION}

ai:
  server:
    url: http://localhost:8000